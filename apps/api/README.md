# apps/api

FastAPI backend for spesto.bg, deployed as a Cloud Run Service.

Not yet scaffolded. Expected: Python 3.12, FastAPI, reads from Cloud SQL Postgres
(canonical products, current prices) and a cached analytics endpoint backed by
BigQuery for price history / inflation queries.
